#Windows setup and run guide

git --version
python --version



git clone https://github.com/abakarovd/tuning_ligthMF.git Tuning
cd Tuning

python -m venv .venv
source .venv/Scripts/activate


python -m pip install --upgrade pip
pip install -r requirements.txt


mkdir -p data/raw
curl -L https://files.grouplens.org/datasets/movielens/ml-latest.zip -o /tmp/ml-latest.zip
curl -L https://files.grouplens.org/datasets/movielens/ml-latest-small.zip -o /tmp/ml-latest-small.zip
unzip -o /tmp/ml-latest.zip -d data/raw
unzip -o /tmp/ml-latest-small.zip -d data/raw

jupyter notebook

#7. Run notebooks in this order

#First:
notebooks/02_preprocessing_and_split.ipynb

#Then:
notebooks/11_1_lightfm_hyperparam_tuning_i9.ipynb


#If lightfm fails to install on Windows, try:


python -m pip install --upgrade pip setuptools wheel
pip install -r requirements.txt
